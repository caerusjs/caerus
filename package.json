{
  "private": true,
  "name": "@caerusjs/monorepo",
  "version": "0.7.0",
  "workspaces": [
    "galileo-next",
    "packages/*"
  ],
  "scripts": {
    "heroku-postbuild": "./utilities/yarnLink.sh && yarn workspace galileo-next build",
    "bootstrap": "yarn install && ./utilities/yarnLink.sh && yarn lerna run build && yarn --cwd galileo install",
    "dev": "yarn concurrently \"yarn --cwd galileo-next dev\" \"yarn uno watch\""
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "lerna": "^3.22.1",
    "unorepo": "^0.1.0"
  }
}
