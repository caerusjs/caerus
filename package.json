{
  "private": true,
  "name": "@caerusjs/monorepo",
  "version": "0.7.0",
  "workspaces": [
    "galileo-next",
    "packages/*"
  ],
  "scripts": {
    "heroku-postbuild": "yarn workspace @caerusjs/helpers build && yarn workspace galileo-next build",
    "bootstrap": "yarn install && yarn lerna run build",
    "dev": "yarn concurrently \"yarn workspace galileo-next dev\" \"yarn uno watch\""
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "lerna": "^3.22.1",
    "unorepo": "^0.1.0"
  },
  "cacheDirectories": [
    ".next/cache"
  ]
}
